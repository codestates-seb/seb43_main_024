spring:
  output:
    ansi:
      enabled: ALWAYS

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: tenten1010!!
    url: jdbc:mysql://ten-ten-preproject-database.cv0dzdrngjmq.ap-northeast-2.rds.amazonaws.com:3306/tenten_app_backend?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC

  config:
    use-legacy-processing: true

  jpa:
    database: mysql
    hibernate:
      ddl-auto: create  # (1) 스키마 자동 생성
    show-sql: true      # (2) SQL 쿼리 출력
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true
        color-codes: true
        use_sql_comments: true
        type:
          descriptor:
            sql: trace
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

  cloud:
    aws:
      stack:
        auto: false

config:
  domain: http://ten-ten.preproject.s3-website.ap-northeast-2.amazonaws.com
